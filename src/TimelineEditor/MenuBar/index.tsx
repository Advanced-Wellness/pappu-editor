import React, { ReactElement } from 'react'

interface Props {}

export default function TimelineEditorMenuBar({}: Props): ReactElement {
  return (
    <div>
      <h1>Timeline Editor Menu Bar</h1>
    </div>
  )
}
